## Notes

- initial={false} prop on AnimatePresence prevents the initial enter animation, ensuring animations only run when the content actually changes. ✅
- Absolutely positioned animated elements don't contribute to layout, so an invisible sizing element is needed to preserve the button's dimensions.
- Keyboard-triggered clicks (event.detail === 0) don't have pointer coordinates, so the ripple should originate from the button's center.